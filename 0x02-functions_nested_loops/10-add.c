#include "main.h"

/**
 * add - adds two integers.
 * @x: the first.
 * @y: the second.
 *
 * Description: adds two integers parameters.
 * Return: the sum of the two integers.
 */

int add(int x, int y)
{
	return (x + y);
}

